{
  "name": "komantle-solver",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/solver.test.mjs",
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  }
}
