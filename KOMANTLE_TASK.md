# 꼬맨틀 솔버 웹 통합 작업 지시서 (정적 사이트, Cloudflare 무료 배포)

> 이 문서는 Claude Code가 그대로 읽고 작업하기 위한 지시서다.
> **각 단계가 끝날 때마다 멈추고 사용자에게 결과를 보고한 뒤 승인을 받고 다음 단계로 넘어간다.**
> 모르는 것은 추측하지 말고 사용자에게 질문한다.

---

## 1. 목표와 핵심 제약

사용자의 프로젝트 웹사이트에 **꼬맨틀 솔버 페이지**를 추가한다.

1. **정답 찾기**: 사용자가 꼬맨틀 화면의 세 숫자(1위·10위·1,000위 유사도)를 입력하면 정답을 찾는다. 정답은 버튼을 눌러야 보인다.
2. **단계별 힌트**: 사용자가 꼬맨틀에서 친 단어(와 유사도)를 입력하면, 그보다 조금 더 유사한 단어를 힌트로 보여준다.

**핵심 제약 (반드시 지킬 것)**

- **서버 코드 없음.** Worker, API 라우트, 서버리스 함수, 데이터베이스(KV·D1·R2 등)를 만들지 않는다.
- **외부 호출 없음.** 꼬맨틀 사이트(semantle-ko.newsjel.ly)나 다른 외부 API를 호출하지 않는다. 크롤링하지 않는다.
- **모든 계산은 브라우저에서** 정적 파일만으로 한다.
- 새 프레임워크나 대형 의존성을 추가하기 전에 사용자에게 묻는다.

---

## 2. 원리

- 꼬맨틀 상단의 세 숫자는 **정답 단어의 1위·10위·1,000위 이웃 유사도 × 100**이다.
- 정답 후보는 4,650개뿐이고, 후보마다 지문 `(1위, 10위, 1000위)`와 상위 1,000개 이웃을 로컬에서 미리 계산해 두었다.
- 입력한 세 숫자와 지문을 대조하면 정답이 특정된다. 검증 결과 1위·10위만으로 후보가 1개로 좁혀졌다.
- **알려진 차이**: 꼬맨틀 서버 어휘가 로컬보다 약간 많아서, 사이트의 1,000위 값이 로컬 지문보다 **같거나 높다.** 1,000위는 한쪽 방향으로만 비교한다. 같은 이유로 로컬 순위는 사이트 순위보다 몇~수십 칸 뒤로 나올 수 있다.

---

## 3. 데이터 (사용자가 로컬에서 생성해 둠)

`export_web_data.py`가 만든 `web_data/komantle-data/` 폴더. 0단계에서 실제 위치를 확인한다.

| 파일 | 형식 | 크기 |
| --- | --- | --- |
| `fp.bin` | Float32 리틀엔디언, 4650×3 행 우선 (`fp[i*3]`=1위, `fp[i*3+1]`=10위, `fp[i*3+2]`=1000위) | 56KB |
| `n/{i}.json` (4,650개) | `{"a": "정답", "n": [["단어", 52.97], ...]}` 유사도 내림차순 1,000개, 인덱스+1 = 로컬 순위, 정답 자신은 제외 | 파일당 약 20KB, 합계 약 100MB |
| `meta.json` | 상수 | 작음 |

- 이 폴더를 사이트의 정적 파일 위치(예: `public/komantle-data/`)에 복사해서 쓴다.
- **데이터 폴더는 git에 넣지 않는다.** 원본은 `web_data/komantle-data/`에 두고(`.gitignore`), 빌드 시 결과물 폴더로 복사해서 배포한다(5-A절).
- 파일 경로를 직접 열면 정답이 보인다. 정답 목록은 원본 레포에 이미 공개돼 있어 새로 노출되는 정보는 아니므로 허용한다. 스포일러는 UI에서 가린다.

---

## 4. 브라우저 로직

### 4-1. 지문 대조

```js
// fp: new Float32Array(await (await fetch(".../fp.bin")).arrayBuffer())
function matchFingerprint(fp, top, top10, rest, tol = 0.006) {
  const exact = [];
  let best = -1, bestErr = Infinity;
  for (let i = 0; i < fp.length / 3; i++) {
    const a = fp[i * 3], b = fp[i * 3 + 1], c = fp[i * 3 + 2];
    const err = Math.max(Math.abs(a - top), Math.abs(b - top10));
    if (err <= tol && rest >= c - tol) exact.push(i);
    if (err < bestErr) { bestErr = err; best = i; }
  }
  return exact.length ? { candidates: exact, approx: false } : { candidates: [best], approx: true };
}
```

- `fp.bin`은 첫 사용 시 한 번만 받아서 메모리에 둔다.
- `rest`(1,000위)는 선택 입력으로 한다. 비어 있으면 1위·10위만으로 대조한다.
- 대조 후 `n/{candidates[0]}.json`을 받아 메모리에 둔다(이후 힌트에 사용).
- 후보가 여러 개면 모두 표시하고 사용자가 고르게 한다. 근사 결과면 "정확히 일치하지 않음, 입력값을 확인하세요" 경고를 띄운다.

### 4-2. 단어 입력 처리

사용자가 꼬맨틀에서 친 단어를 입력한다.

1. 단어 == 정답이면 "정답!" 표시.
2. 단어가 이웃 1,000개 안에 있으면 **유사도를 자동으로 채운다**(순위도 "약 N위"로 표시). 사용자가 유사도를 입력할 필요 없음.
3. 이웃 목록에 없으면 "꼬맨틀에 나온 유사도를 입력해주세요" 입력칸을 띄운다. 입력값으로 힌트를 계산한다.

### 4-3. 힌트

```js
function hints(neighbors, guessSim, step, n = 5, exclude = new Set()) {
  const target = guessSim + step;
  return neighbors
    .map(([word, sim], i) => ({ word, sim, rank: i + 1 }))
    .filter(h => h.sim > guessSim && !exclude.has(h.word))
    .sort((x, y) => Math.abs(x.sim - target) - Math.abs(y.sim - target))
    .slice(0, n)
    .sort((x, y) => y.sim - x.sim);
}
```

- `step` 선택지: 조금(+2), 보통(+5), 많이(+10)
- `exclude`: 지금까지 입력한 단어들
- `guessSim`이 1위 이웃 유사도 이상이면 "거의 다 왔어요"를 표시한다.
- 결과 표시 직전에 정답 단어가 없는지 한 번 더 걸러낸다.

---

## 5. 화면 구성

한 페이지에 세 영역을 둔다.

1. **세 숫자 입력**
   - 1위, 10위(필수), 1,000위(선택) 입력칸과 "찾기" 버튼
   - 사용법 안내: "꼬맨틀 첫 화면 상단 문장의 숫자를 그대로 입력하세요"
   - 입력칸에 문장 전체를 붙여넣으면 숫자 세 개를 자동 추출해 채우는 기능을 넣는다. 예: "정답 단어와 가장 유사한 단어의 유사도는 52.97 입니다. 10번째로 … 45.92이고, 1,000번째로 … 29.64 입니다." → 52.97 / 45.92 / 29.64. 회차 번호가 포함돼 있으면 표시용으로만 쓴다.
2. **정답 보기**
   - 찾기 성공 후 활성화. 기본은 가려진 상태, 클릭 → 확인 대화상자("스포일러입니다") → 표시
3. **힌트 받기**
   - 단어 입력, (필요할 때만) 유사도 입력, 힌트 단계 선택, 제출
   - 결과: 내 단어 유사도·순위, 힌트 5개(클릭하면 입력창에 채움)
   - 입력한 단어 기록 표(유사도 내림차순)
   - 세 숫자와 기록은 `localStorage`에 저장한다. 다른 세 숫자가 입력되면 기록을 초기화한다.

하단 출처 표기: "데이터·규칙 출처: 꼬맨틀(뉴스젤리), FastText". 원본 레포의 LICENSE 파일 요구사항을 확인해 반영한다.

기존 사이트가 있으면 그 디자인·라우팅·컴포넌트를 따른다.

---

## 5-A. 배포: Cloudflare Workers Static Assets (무료)

사용자는 **전부 Cloudflare**에 배포하기로 했다. 사이트 전체와 데이터 폴더를 하나의 **정적 자산 전용 Worker**로 올린다(Worker 스크립트 없음). 새 프로젝트라면 Pages 대신 이 방식을 쓴다. 기존에 Pages 프로젝트가 있으면 0단계에서 보고하고 사용자에게 묻는다.

### 무료 플랜 제한 (구현 전에 공식 문서로 재확인)

| 항목 | 한도 | 이 프로젝트 |
| --- | --- | --- |
| Worker 버전당 정적 파일 수 | 20,000개 | 약 4,650개 + 사이트 파일 |
| 파일당 크기 | 25MiB | 최대 수십 KB |
| 정적 파일 요청 | 무료 무제한 | - |

### 설정 예시 (`wrangler.jsonc`)

```jsonc
{
  "name": "komantle-solver",          // 사용자와 정한 이름
  "compatibility_date": "YYYY-MM-DD", // 작업 당일 날짜
  "assets": {
    "directory": "./dist",            // 프레임워크의 빌드 결과물 폴더
    "not_found_handling": "single-page-application" // SPA 라우팅일 때만. 아니면 제거
  }
}
```

- `main`(Worker 스크립트)은 넣지 않는다.
- 빌드 결과물 폴더 이름은 프레임워크에 맞춘다(Vite `dist`, Next.js 정적 export `out` 등). Next.js라면 서버 기능 없이 `output: "export"`로 정적 빌드가 되는지 확인한다.

### 데이터 복사

- 빌드 후 `web_data/komantle-data/`를 결과물 폴더의 `komantle-data/`로 복사하는 스크립트를 만든다. Windows PowerShell에서 동작해야 한다(Node 스크립트 권장).
- 원본 폴더가 없으면 "먼저 `python export_web_data.py`를 실행하세요"라는 메시지와 함께 실패시킨다.

### 캐시 헤더 (`_headers`, 결과물 폴더 루트에 위치)

```
/komantle-data/*
  Cache-Control: public, max-age=86400
```

### 배포 명령

- `package.json`에 스크립트를 만든다: `build`(사이트 빌드 + 데이터 복사), `deploy`(`build` 후 `npx wrangler deploy`).
- **배포는 로컬에서만 한다.** 데이터가 git에 없으므로 GitHub 연동 자동 배포(Workers Builds, Pages Git 연동)는 설정하지 않는다.
- `npx wrangler login`은 사용자가 직접 실행한다. 로그인이 필요하면 멈추고 요청한다.

---

## 6. 작업 단계

### 0단계: 탐색과 계획 (코드 수정 금지)
- 프로젝트 구조, 프레임워크, 빌드 결과물 폴더, 기존 배포 설정(`wrangler.toml`/`wrangler.jsonc`, `vercel.json` 등)을 파악한다.
- `web_data/komantle-data/` 위치와 파일 수·크기를 확인한다.
- 사이트가 서버 기능 없이 정적 빌드 가능한지 확인한다. 서버 기능(SSR, API 라우트 등)이 있으면 멈추고 보고한다.
- `.gitignore`에 `web_data/`, `*.pkl`, `*.npz`, 빌드 결과물 폴더가 있는지 확인한다.
- 5-A절 기준으로 통합 계획(파일 목록, 페이지 경로, 빌드·복사·배포 스크립트)을 보고하고 **승인을 받는다.**

### 1단계: 로직 모듈
- 4절 함수를 별도 모듈로 구현한다(`matchFingerprint`, `hints`, 문장에서 숫자 추출, 데이터 로더).
- 테스트(기존 테스트 도구가 있으면 사용, 없으면 사용자에게 묻거나 간단한 Node 스크립트):
  - `(52.97, 45.92, 29.64)` → `candidates=[1630]`, `approx=false`
  - `(52.97, 45.92, 빈값)` → `candidates=[1630]`
  - 꼬맨틀 안내 문장 붙여넣기 → 세 숫자 정확히 추출
  - 힌트 결과에 정답 없음, `exclude` 반영, 전부 `guessSim`보다 높음
- **정답 단어를 테스트 출력·로그에 찍지 않는다.** 비교 결과(true/false)만 출력한다.

### 2단계: 화면
- 5절대로 구현한다. 모바일 화면을 확인한다.
- 네트워크 탭에서 `fp.bin` 1회, `n/{i}.json` 1회만 요청되는지, 외부 도메인 요청이 없는지 확인한다.

### 3단계: 배포
- 5-A절대로 `wrangler.jsonc`, `_headers`, 데이터 복사 스크립트, `build`/`deploy` 스크립트를 만든다.
- `npm run build` 후 결과물 폴더의 전체 파일 수와 가장 큰 파일 크기를 보고한다(20,000개, 25MiB 이내).
- `npx wrangler dev`로 로컬에서 정적 서빙을 확인한다(데이터 파일 경로, `fp.bin` 로딩, 1630 테스트).
- `npx wrangler whoami`로 로그인을 확인하고, 안 되어 있으면 사용자에게 `npx wrangler login`을 요청하고 멈춘다.
- **사용자 승인 후** `npm run deploy`를 실행하고, 배포된 `*.workers.dev` 주소에서 세 기능과 외부 요청 0건을 점검한다.
- README에 데이터 생성 순서(`precompute.py` → `export_web_data.py`), `npm run deploy` 사용법, 데이터를 다시 만들었을 때 재배포 방법을 정리한다.

---

## 7. 금지사항

- 서버 코드, 서버리스 함수, 데이터베이스·스토리지 서비스(KV·D1·R2 등) 추가
- 데이터 폴더를 git에 커밋하거나 Git 연동 자동 배포를 설정하는 것
- 꼬맨틀 사이트를 포함한 모든 외부 API 호출·크롤링
- 날짜로 회차를 계산해 정답 파일을 자동으로 여는 기능 (사용자가 세 숫자를 입력해야만 정답을 찾는다)
- 정답 단어를 로그, 콘솔, 테스트 출력, 커밋 메시지에 남기는 것
- 사용자 승인 없이 새 프레임워크·대형 의존성 추가

## 8. 완료 조건

- [ ] 세 숫자(또는 안내 문장 붙여넣기)로 정답을 찾고, 1630회차 기준 결과가 정확하다
- [ ] 정답은 확인 버튼을 거쳐야만 보인다
- [ ] 이웃 목록에 있는 단어는 유사도 입력 없이 힌트가 나오고, 없는 단어는 유사도 입력으로 힌트가 나온다
- [ ] 힌트에 정답이 포함되지 않는다
- [ ] 외부 도메인 요청이 0건이다
- [ ] `npm run deploy` 한 번으로 Cloudflare에 배포되고, `*.workers.dev` 주소에서 동작한다
- [ ] README에 데이터 준비·배포·사용 방법이 있다
